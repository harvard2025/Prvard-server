from django.contrib import admin
from .models import E_Message, Class, Chat_Message, C_P_real, Content, Login, University, Student, P_U_real, C_U_real, Weeks
# Register your models here.

admin.site.register(Class)
admin.site.register(E_Message)
admin.site.register(Chat_Message)
admin.site.register(C_P_real)
admin.site.register(Content)
admin.site.register(Login)
admin.site.register(University)
admin.site.register(Student)
admin.site.register(P_U_real)
admin.site.register(C_U_real)
admin.site.register(Weeks)










    # + Create model E_Message
    # + Create model Class
    # + Create model Chat_Message
    # + Create model C_P_real
    # + Create model Content
    # + Create model Login
    # + Create model University
    # + Create model Student
    # + Create model P_U_real
    # + Create model C_U_real
    # + Create model Weeks
